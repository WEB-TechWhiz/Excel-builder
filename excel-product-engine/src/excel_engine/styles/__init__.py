"""Design system — colors, typography, borders, fills, alignment, and
the StyleManager that ties them together per theme.

    from excel_engine.styles import StyleManager
"""

from excel_engine.styles.style_manager import Spacing, StyleManager

__all__ = ["StyleManager", "Spacing"]
